services:
  - type: web
    name: felgen-api
    runtime: python
    buildCommand: |
      pip install -r requirements.txt
      playwright install
    startCommand: python app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.10
